subdir('ata_pio')
subdir('early_serial')
subdir('cmos')
subdir('pci')
subdir('ps2_controller')
subdir('kb')

# Export driver libraries to parent scope
driver_libs = [
  libata_pio,
  libearly_serial,
  libcmos,
  libpci,
  libps2_controller,
  libkb,
]
